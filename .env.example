# Database (Supabase Postgres)
# 推荐：显式设置 DATABASE_URL 与 DIRECT_URL
DATABASE_URL="postgres://<user>.<ref>:<password>@aws-1-<region>.pooler.supabase.com:6543/<database>?sslmode=require&pgbouncer=true"
DIRECT_URL="postgres://<user>.<ref>:<password>@aws-1-<region>.pooler.supabase.com:5432/<database>?sslmode=require"

# 或者：使用 Supabase/Vercel 默认变量名（我们的代码会自动回退到这些变量）
# 如果你不设置 DATABASE_URL，至少设置以下其中之一即可正常连接
POSTGRES_PRISMA_URL="postgres://<user>.<ref>:<password>@aws-1-<region>.pooler.supabase.com:6543/<database>?sslmode=require&pgbouncer=true"
POSTGRES_URL="postgres://<user>.<ref>:<password>@aws-1-<region>.pooler.supabase.com:6543/<database>?sslmode=require&pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgres://<user>.<ref>:<password>@aws-1-<region>.pooler.supabase.com:5432/<database>?sslmode=require"

# NextAuth（必须）
NEXTAUTH_URL="https://<你的 Vercel 域名>.vercel.app"
NEXTAUTH_SECRET="<生成一个随机安全字符串>"

# Supabase 前端（如需在前端直接使用 Supabase 客户端）
NEXT_PUBLIC_SUPABASE_URL="https://<your-supabase-project>.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<你的 Supabase anon key>"

# Supabase 服务端（可选，仅服务端使用；目前项目未直接使用此 key）
SUPABASE_SERVICE_ROLE_KEY="<你的 service role key>"
SUPABASE_JWT_SECRET="<你的 JWT secret>"

#（可选）文件上传，如使用 Vercel Blob
#BLOB_READ_WRITE_TOKEN="<your-vercel-blob-token>"