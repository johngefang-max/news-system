'use client';

import ArticleEditorPage from '../[id]/page';

export default function NewArticlePage() {
  // New article page uses the same editor component but without editing mode
  return <ArticleEditorPage />;
}